package com.naviksha.service;

import com.naviksha.model.StudentReport;
import lombok.extern.slf4j.Slf4j;
package com.naviksha.service;

/**
 * EmailService removed: project uses SMS-only OTP. This class remains as a no-op placeholder
 * to avoid compile errors in places that may still reference it during transition.
 */
public class EmailService {
    // intentionally left blank
}